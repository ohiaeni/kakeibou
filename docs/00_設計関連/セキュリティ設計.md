# セキュリティ設計

- [セキュリティ設計](#セキュリティ設計)
  - [セキュリティ概要](#セキュリティ概要)
  - [脅威モデル](#脅威モデル)
    - [1. 想定される脅威](#1-想定される脅威)
    - [2. 資産の分類](#2-資産の分類)
  - [認証・認可設計](#認証認可設計)
    - [1. 認証方式](#1-認証方式)
      - [JWT（JSON Web Token）ベース認証](#jwtjson-web-tokenベース認証)
      - [JWT トークン設計](#jwt-トークン設計)
  - [入力検証・サニタイゼーション](#入力検証サニタイゼーション)
    - [1. バリデーション設計](#1-バリデーション設計)
      - [Bean Validation使用](#bean-validation使用)
      - [カスタムバリデーター](#カスタムバリデーター)
    - [2. SQLインジェクション対策](#2-sqlインジェクション対策)
      - [MyBatis パラメータバインディング](#mybatis-パラメータバインディング)
  - [セッション管理](#セッション管理)
    - [1. JWT トークン管理](#1-jwt-トークン管理)
      - [トークンのライフサイクル](#トークンのライフサイクル)
    - [2. セッション固定攻撃対策](#2-セッション固定攻撃対策)
  - [データ保護](#データ保護)
    - [1. 暗号化](#1-暗号化)
      - [データベース暗号化](#データベース暗号化)
      - [パスワードハッシュ化](#パスワードハッシュ化)
    - [2. HTTPS強制](#2-https強制)
  - [CSRF/XSS対策](#csrfxss対策)
    - [1. CSRF対策](#1-csrf対策)
    - [2. XSS対策](#2-xss対策)

## セキュリティ概要

家計簿アプリケーションは、ユーザーの個人的な財務情報を扱うため、高いレベルのセキュリティが要求されます。本ドキュメントでは、アプリケーションのセキュリティ設計について詳述します。

## 脅威モデル

### 1. 想定される脅威

| 脅威                   | 影響度 | 対策優先度 | 説明                             |
| ---------------------- | ------ | ---------- | -------------------------------- |
| 不正ログイン           | 高     | 高         | 他人のアカウントへの不正アクセス |
| SQLインジェクション    | 高     | 高         | データベースへの不正アクセス     |
| XSS攻撃                | 中     | 高         | 悪意のあるスクリプト実行         |
| CSRF攻撃               | 中     | 中         | 意図しない操作の実行             |
| データ漏洩             | 高     | 高         | 個人情報の外部流出               |
| セッションハイジャック | 中     | 中         | セッションの乗っ取り             |

### 2. 資産の分類

| 資産             | 機密性 | 完全性 | 可用性 |
| ---------------- | ------ | ------ | ------ |
| ユーザー認証情報 | 極高   | 高     | 高     |
| 支出記録データ   | 高     | 高     | 中     |
| 予算設定データ   | 高     | 高     | 中     |
| システム設定     | 中     | 高     | 高     |

## 認証・認可設計

### 1. 認証方式

#### Spring Security フォームベース認証
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(authz -> authz
                .requestMatchers("/login", "/register", "/css/**", "/js/**", "/images/**").permitAll()
                .anyRequest().authenticated()
            )
            .formLogin(form -> form
                .loginPage("/login")
                .defaultSuccessUrl("/dashboard", true)
                .failureUrl("/login?error=true")
                .permitAll()
            )
            .logout(logout -> logout
                .logoutUrl("/logout")
                .logoutSuccessUrl("/login?logout=true")
                .invalidateHttpSession(true)
                .deleteCookies("JSESSIONID")
                .permitAll()
            )
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.IF_REQUIRED)
                .maximumSessions(1)
                .maxSessionsPreventsLogin(false)
                .sessionRegistry(sessionRegistry())
            )
            .csrf(csrf -> csrf
                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
            );
        
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder(12);
    }

    @Bean
    public SessionRegistry sessionRegistry() {
        return new SessionRegistryImpl();
    }
}
```

#### セッションベース認証設計
- **セッション管理**: HTTPセッションを使用
- **セッション有効期限**: 30分（アクティビティによる自動延長）
- **同時セッション制御**: 1ユーザーあたり1セッションまで

## 入力検証・サニタイゼーション

### 1. バリデーション設計

#### Bean Validation使用
```java
public class CreateExpenseRequest {
    
    @NotNull(message = "カテゴリIDは必須です")
    private UUID categoryId;
    
    @NotNull(message = "金額は必須です")
    @Min(value = 1, message = "金額は1円以上である必要があります")
    @Max(value = 10000000, message = "金額は1000万円以下である必要があります")
    private Integer amount;
    
    @Size(max = 1000, message = "説明は1000文字以内で入力してください")
    private String description;
    
    @NotNull(message = "支出日は必須です")
    @PastOrPresent(message = "支出日は現在日以前である必要があります")
    private LocalDate expenseDate;
}
```

#### カスタムバリデーター
```java
@Component
public class ExpenseValidator {
    
    public void validateExpenseCreation(CreateExpenseRequest request) {
        if (request.getExpenseDate().isAfter(LocalDate.now())) {
            throw new ValidationException("未来の日付は設定できません");
        }
        
        // SQLインジェクション対策
        if (containsSqlKeywords(request.getDescription())) {
            throw new ValidationException("不正な文字が含まれています");
        }
    }
    
    private boolean containsSqlKeywords(String input) {
        String[] sqlKeywords = {"DROP", "DELETE", "INSERT", "UPDATE", "SELECT"};
        String upperInput = input.toUpperCase();
        return Arrays.stream(sqlKeywords).anyMatch(upperInput::contains);
    }
}
```

### 2. SQLインジェクション対策

#### MyBatis パラメータバインディング
```xml
<!-- 安全な書き方 -->
<select id="findExpensesByUserId" parameterType="UUID" resultType="ExpenseEntity">
    SELECT * FROM expense_records 
    WHERE user_id = #{userId}
    AND expense_date BETWEEN #{startDate} AND #{endDate}
</select>

<!-- 危険な書き方（使用禁止）-->
<select id="unsafeQuery" parameterType="String" resultType="ExpenseEntity">
    SELECT * FROM expense_records WHERE ${condition}
</select>
```

## セッション管理

### 1. HTTPセッション管理

#### セッションライフサイクル
```java
@Configuration
public class SessionConfig implements WebMvcConfigurer {
    
    @Bean
    public SessionRegistry sessionRegistry() {
        return new SessionRegistryImpl();
    }
    
    @Bean
    public HttpSessionEventPublisher httpSessionEventPublisher() {
        return new HttpSessionEventPublisher();
    }
    
    @EventListener
    public void onSessionCreated(HttpSessionCreatedEvent event) {
        HttpSession session = event.getSession();
        session.setMaxInactiveInterval(30 * 60); // 30分
        log.info("Session created: {}", session.getId());
    }
    
    @EventListener
    public void onSessionDestroyed(HttpSessionDestroyedEvent event) {
        log.info("Session destroyed: {}", event.getSession().getId());
    }
}
```

### 2. セッション固定攻撃対策

```java
@Configuration
public class SessionSecurityConfig {
    
    @Bean
    public SessionFixationProtectionStrategy sessionFixationProtectionStrategy() {
        return new SessionFixationProtectionStrategy();
    }
    
    @Bean
    public ConcurrentSessionControlAuthenticationStrategy concurrentSessionControlStrategy() {
        ConcurrentSessionControlAuthenticationStrategy strategy = 
            new ConcurrentSessionControlAuthenticationStrategy(sessionRegistry());
        strategy.setMaximumSessions(1);
        strategy.setExceptionIfMaximumExceeded(false);
        return strategy;
    }
}
```

## データ保護

### 1. 暗号化

#### データベース暗号化
```java
@Entity
@Table(name = "user_accounts")
public class UserEntity {
    
    @Column(name = "email")
    @Convert(converter = EncryptionConverter.class)
    private String email;
    
    @Column(name = "password_hash")
    private String passwordHash; // BCryptでハッシュ化済み
}

@Converter
public class EncryptionConverter implements AttributeConverter<String, String> {
    
    @Autowired
    private EncryptionService encryptionService;
    
    @Override
    public String convertToDatabaseColumn(String attribute) {
        return encryptionService.encrypt(attribute);
    }
    
    @Override
    public String convertToEntityAttribute(String dbData) {
        return encryptionService.decrypt(dbData);
    }
}
```

#### パスワードハッシュ化
```java
@Service
public class PasswordService {
    
    private final PasswordEncoder passwordEncoder = new BCryptPasswordEncoder(12);
    
    public String hashPassword(String plainPassword) {
        return passwordEncoder.encode(plainPassword);
    }
    
    public boolean verifyPassword(String plainPassword, String hashedPassword) {
        return passwordEncoder.matches(plainPassword, hashedPassword);
    }
}
```

### 2. HTTPS強制

```java
@Configuration
public class HttpsConfig {
    
    @Bean
    public TomcatServletWebServerFactory servletContainer() {
        TomcatServletWebServerFactory tomcat = new TomcatServletWebServerFactory() {
            @Override
            protected void postProcessContext(Context context) {
                SecurityConstraint constraint = new SecurityConstraint();
                constraint.setUserConstraint("CONFIDENTIAL");
                SecurityCollection collection = new SecurityCollection();
                collection.addPattern("/*");
                constraint.addCollection(collection);
                context.addConstraint(constraint);
            }
        };
        tomcat.addAdditionalTomcatConnectors(redirectConnector());
        return tomcat;
    }
}
```

## CSRF/XSS対策

### 1. CSRF対策

```java
@Configuration
public class CsrfConfig {
    
    @Bean
    public CsrfTokenRepository csrfTokenRepository() {
        CookieCsrfTokenRepository repository = CookieCsrfTokenRepository.withHttpOnlyFalse();
        repository.setCookieName("XSRF-TOKEN");
        repository.setHeaderName("X-XSRF-TOKEN");
        return repository;
    }
}
```

**Thymeleafテンプレートでの使用例**:
```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta name="_csrf" th:content="${_csrf.token}"/>
    <meta name="_csrf_header" th:content="${_csrf.headerName}"/>
</head>
<body>
    <form action="/expenses" method="post">
        <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>
        <!-- フォームフィールド -->
        <input type="submit" value="送信"/>
    </form>
</body>
</html>
```

### 2. XSS対策

```java
@Component
public class XssFilterService {
    
    private final Policy policy;
    
    public XssFilterService() {
        this.policy = new PolicyFactory()
            .allowElements("b", "i", "em", "strong", "br")
            .allowUrlProtocols("https")
            .allowAttributes("class").onElements("span")
            .toFactory();
    }
    
    public String sanitize(String input) {
        if (input == null) return null;
        return policy.sanitize(input);
    }
}
```

**Thymeleafテンプレートでの自動エスケープ**:
```html
<!-- 自動エスケープ（推奨） -->
<p th:text="${userInput}">ユーザー入力</p>

<!-- HTMLとして出力（注意深く使用） -->
<p th:utext="${sanitizedHtml}">サニタイズ済みHTML</p>

<!-- 属性値のエスケープ -->
<input type="text" th:value="${userInput}" />
```

@ControllerAdvice
public class ResponseBodySanitizer implements ResponseBodyAdvice<Object> {
    
    @Override
    public Object beforeBodyWrite(Object body, 
                                 MethodParameter returnType,
                                 MediaType selectedContentType,
                                 Class<? extends HttpMessageConverter<?>> selectedConverterType,
                                 ServerHttpRequest request,
                                 ServerHttpResponse response) {
        // レスポンスデータのサニタイズ
        return sanitizeObject(body);
    }
}
```
